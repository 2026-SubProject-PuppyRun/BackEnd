# PuppyRun Codex 지침

이 파일은 저장소 전체에 적용되는 Codex 작업 지침입니다.

## 프로젝트 환경

- Java 21과 Spring Boot 3.3.5를 사용합니다.
- 빌드와 테스트는 Gradle Wrapper(`./gradlew`)로 실행합니다.
- 작업 전 `git status --short`를 확인하고, 사용자가 만든 관련 없는 변경은 수정하거나 되돌리지 않습니다.
- 파일 탐색과 검색은 가능한 한 `rg --files`와 `rg`를 우선 사용합니다.

## 구현 규칙

- 요청·응답 DTO는 불변성이 적합한 경우 `record`를 우선 검토합니다.
- JPA Entity는 프로젝트의 `BaseEntity` 상속 및 기존 생성·수정 패턴을 따릅니다.
- 비즈니스 오류는 공통 `BusinessException`과 `ErrorCode` 체계를 사용합니다.
- 테스트 코드 작성, 수정, 리팩터링 또는 검토 요청을 받으면 [테스트 작성 컨벤션](.codex/conventions/test.md)을 먼저 읽고 테스트 유형, Fixture, TestData 및 실행 규칙을 따릅니다.
- 변경 범위에 맞는 테스트를 먼저 실행하고, 영향 범위가 넓으면 `./gradlew test`로 전체 회귀를 확인합니다.
- 테스트를 실행하지 못했거나 실패가 남아 있으면 완료 응답에 이유와 영향을 명시합니다.

## 커밋과 PR

- 커밋 메시지 작성 또는 커밋 요청을 받으면 [커밋 메시지 규칙](.codex/conventions/commit.md)을 읽고 따릅니다.
- PR 설명 작성 요청을 받으면 [PR 작성 규칙](.codex/conventions/pull-request.md)과 [.github/pull_request_template.md](.github/pull_request_template.md)를 읽고 따릅니다.
- 커밋, 푸시, PR 생성처럼 외부 상태를 변경하는 작업은 사용자가 명시적으로 요청한 범위에서만 수행합니다.

## 서브에이전트

Codex 사용자 정의 에이전트는 `.codex/agents/*.toml`에 정의되어 있습니다.

- `code_reviewer`: 큰 변경이나 현재 브랜치의 품질·보안·성능·회귀 위험을 독립적으로 검토할 때 사용합니다.
- `project_manager`: 아키텍처 대안, 기술 선택, 우선순위, 단계별 로드맵을 결정할 때 사용합니다.
- 사용자가 특정 에이전트나 병렬 작업을 요청하면 해당 역할에 경계가 분명한 하위 작업을 위임하고, 결과를 기다린 뒤 하나의 결론으로 통합합니다.
- 여러 에이전트가 동시에 같은 파일을 수정하지 않도록 합니다. 탐색과 검토는 병렬화할 수 있지만 구현 파일의 소유권은 분리합니다.

## Code Review Rules

- 스타일보다 정확성, 보안, 데이터 정합성, 동시성, 성능 회귀, 누락된 테스트를 우선합니다.
- 발견 사항에는 재현 가능한 조건, 실제 영향, 근거가 되는 파일과 줄, 최소 수정 방향을 포함합니다.
- 근거가 없는 가능성은 결함으로 단정하지 않습니다.
